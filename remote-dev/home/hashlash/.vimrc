" Get the defaults that most users want.
source $VIMRUNTIME/defaults.vim

" General indentation
set tabstop=8 softtabstop=0 shiftwidth=4 expandtab smarttab

" Display tab and trailing whitespace
set list
set listchars=tab:›\ ,trail:·
